[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "rnascope-counter"
version = "0.1.0"
description = "User interface for spot counting and intensity calculation for RNAScope analysis."
readme = "README.md"
authors = [{name = "AutoGenerated"}]
dependencies = [
    "napari",
    "numpy",
    "pandas",
    "scikit-image",
    "magicgui",
]

[project.optional-dependencies]
dev = ["pytest"]
